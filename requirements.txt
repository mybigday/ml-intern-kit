# Pip-installable mirror of [project.dependencies] in pyproject.toml.
# Keep this in sync with pyproject.toml. Use `bootstrap.sh` to install.
#
# Core training stack
torch>=2.6
transformers>=5.5
trl>=0.28
peft>=0.18
accelerate>=1.12
datasets>=4.7
evaluate>=0.4.3
tokenizers>=0.21
safetensors>=0.5
sentencepiece>=0.2
protobuf>=4.25
einops>=0.8
# bitsandbytes wheels on PyPI are CUDA-only. For gfx1151, build from source
# with `cmake -DCOMPUTE_BACKEND=hip -DBNB_ROCM_ARCH=gfx1151` (see env-bootstrap skill).
bitsandbytes>=0.49; platform_system == "Linux" and platform_machine == "x86_64"

# Hub + data + I/O
huggingface_hub[cli,hf_transfer]>=1.11
hf_transfer>=0.1.8
pyarrow>=17.0
pandas>=2.2
numpy>=1.26,<3
scipy>=1.13
scikit-learn>=1.5

# Monitoring
trackio>=0.24
tensorboard>=2.18

# Research / agent helpers
requests>=2.32
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.9
rich>=13.9
typer>=0.12
pyyaml>=6.0.2
tenacity>=9.0
arxiv>=2.1
